db:
#  dsn: <user>:<pass>@tcp(<host>:<port>)/<database>?parseTime=true
  dsn: dockeruser:dockeruser@tcp(mysqldb:3306)/cp_dev?parseTime=true
  migration_path: "file://db/migrations/schema" # Relative
  seed_path: "db/migrations/seed" # Relative
gov_sg_service:
  base_url: "https://api.data.gov.sg/"
misc:
  environment: "development"